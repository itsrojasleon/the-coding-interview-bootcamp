<span className='instructions'>
  Write a function that accepts a positive number N. The function should console
  log a pyramid shape with N levels using the # character. Make sure the pyramid
  has spaces on both the left *and* right hand sides
</span>

<div className='instructions example'>Examples</div>

<div className='instructions'>pyramid(1)</div>
<div className='instructions'>'#'</div>

<div className='instructions'>pyramid(2)</div>
<div className='instructions'>' # '</div>
<div className='instructions'>'###'</div>

<div className='instructions'>pyramid(3)</div>
<div className='instructions'>'  #  '</div>
<div className='instructions'>' ### '</div>
<div className='instructions'>'#####'</div>

```
// First (🥦)

function pyramid(n) {
  const midpoint = Math.floor((2 * n - 1) / 2)

  for (let row = 0; row < n; row++) {
    let level = '';

    for (let column = 0; column < 2 * n -1; column++) {
      if (midpoint - row <= column && midpoint + row >= column) {
        level += '#';
      } else {
        level += ' '
      }
    }
    console.log(level);
  }
}


// Second (🥒)


```
