// --- Directions
// Given a string, return a new string with the reversed
// order of characters
// --- Examples
//   reverse('apple') === 'leppa'
//   reverse('hello') === 'olleh'
//   reverse('Greetings!') === '!sgniteerG'

// This is a way
// Very simple using reverse()

/* function reverse(str) {
  return str
    .split('')
    .reverse()
    .join('');
}*/

// Second Way
function reverse() {}
module.exports = reverse;
